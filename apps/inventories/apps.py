"""Inventories app."""

# Django
from django.apps import AppConfig


class InventoriesAppConfig(AppConfig):
    """Inventories app config."""
    
    name = 'apps.inventories'
    verbose_name = 'Inventarios'

